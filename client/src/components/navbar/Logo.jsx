import { Text } from "@chakra-ui/react";

const Logo = () => {
  return (
    <Text fontSize={{ base: "2xl", lg: "3xl" }} fontWeight="semibold">
      Chatify
    </Text>
  );
};

export default Logo;
